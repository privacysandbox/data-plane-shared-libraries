The top **src** directory is intended to include the public api to talk to a worker process running
within a sandbox. Anything in other directiories is considered private and should not be included
from other modules.
